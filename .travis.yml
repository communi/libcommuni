language: cpp
compiler:
  - gcc
  - clang
script:
  - qmake -v
  - sudo apt-get install libicu-dev
  - ./configure -config private_tests -icu
  - make
  - make check
  - sudo make install
  - cd tests/build
  - qmake && make
notifications:
  email: false
  irc:
    channels:
      - "irc.freenode.org#communi"
    template:
     - "%{repository}#%{build_number} (%{branch} - %{commit} : %{author}): %{message} - %{build_url}"
    use_notice: true
    skip_join: true
